apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: name
  namespace: default
spec:
  rules:
    - host: example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: service-name
                port:
                  number: 80